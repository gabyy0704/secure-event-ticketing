# NIKADA ne commitati ovu datoteku s pravim vrijednostima!
# Kreirati Secret rucno ili kroz CI/CD pipeline:
#
#   kubectl create secret generic db-secret \
#     --from-literal=POSTGRES_PASSWORD=<lozinka> \
#     -n ticketing
#
# Primjer datoteke s placeholder vrijednostima (base64 od "change_me"):
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: ticketing
type: Opaque
data:
  # echo -n 'change_me_local' | base64
  POSTGRES_PASSWORD: Y2hhbmdlX21lX2xvY2Fs
